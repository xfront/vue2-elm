// Add your native dependencies here:

android {
  defaultConfig {
    generatedDensities = []
    applicationId = "com.github.xfront.elm"
  }
  aaptOptions {
    additionalParameters "--no-version-vectors"
  }
}
